import 'package:flutter/material.dart';

class ScheduleDayItem extends StatelessWidget {
  const ScheduleDayItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
