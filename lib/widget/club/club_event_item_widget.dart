import 'package:flutter/material.dart';

class ClubEventItem extends StatelessWidget {
  const ClubEventItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("ClubEventItem");
  }
}
